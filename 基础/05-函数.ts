// hello 函数

function hello(name: string): string {
    return "Hi," + name;
}

console.log(hello("sifan"));

// add 函数

function add(x: number, y: number): number {
    return x + y;
}

console.log(add(1, 3));

// void 函数

function ll(name: string): void {
    console.log("void=======>" + name)
}

ll("shentu");
